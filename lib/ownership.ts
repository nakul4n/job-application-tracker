export function ownedApplication(userId: string, id: string) {
  return { id, userId };
}

export function ownedContact(userId: string, id: string) {
  return { id, userId };
}

export function ownedFollowUp(userId: string, id: string) {
  return { id, userId };
}

export function ownedNote(userId: string, id: string) {
  return { id, userId };
}
